import {HaxObject} from '/x/HaxObject.js';
import {Store} from '/x/data/Store.js';

/**
 * Grid. Kinda what you'd expect. Useful for displaying data in a, you know, grid-like fashion.
 */
export default class Grid extends HaxObject {
	constructor(config) {
		
	}

	set store
}